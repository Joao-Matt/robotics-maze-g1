# Copy this file to .env.storage and set values for the local machine.
# The local file is ignored so machine-specific mount paths never enter Git.
REQUIRED_STORAGE_MOUNT=/path/to/external/storage
EXPECTED_STORAGE_UUID=replace-with-filesystem-uuid
